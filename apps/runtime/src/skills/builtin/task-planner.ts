import type { SkillDefinition } from '../types.js'

/**
 * 任务规划：把多步骤任务显式化为"计划清单 → 逐项执行并勾选 → 对照总结"，
 * 避免头绪复杂的任务在长改动中失去方向（Plan/Execute 模式的技能级实现）。
 * 注意：计划本身不落盘，本轮会话内按模型输出维护（见 AGENT-ENHANCEMENTS #6）。
 */
export const TASK_PLANNER_SKILL: SkillDefinition = {
  manifest: {
    id: 'task-planner',
    name: '任务规划',
    version: '1.0.0',
    description:
      '把多步骤任务拆成带执行标记的计划清单：先输出计划，逐项执行并勾选，最后给出对照总结与未完成项。',
    tools: ['file.read', 'file.grep', 'file.glob', 'shell.execute'],
    argumentHint: '<任务描述>',
  },
  instructions: `
# 任务规划

适用于多文件、多步骤或结果需要验收的任务；单步任务不必使用。

## 步骤

1. **输出计划清单**：先读必要上下文（项目结构、相关文件），再输出编号清单，每项写明——做什么、涉及哪些文件/命令、完成标准（如"测试通过""编译通过"）。
2. **逐项执行**：按清单顺序执行；每完成一项，在清单对应行标记"✅ 已达成"并附一句证据（输出/文件变更）；失败则标记"❌"与原因，不要跳过不记。
3. **动态调整**：执行中发现原计划不合理（缺步骤/步骤多余），先向用户说明调整与理由，再更新清单继续。
4. **对照总结**：结束前输出最终清单（✅/❌ 逐项），说明未完成项与原因；与目标不一致的结果要明确指出。

## 纪律

- 清单必须具体可验收，禁止"做好 X"这类无法验证的描述。
- 每项独立且尽量小，避免一项里混多个改动。
- 与用户更改目标无关的工作不做；发现范围扩大时回到步骤 3 说明。
`.trim(),
}
