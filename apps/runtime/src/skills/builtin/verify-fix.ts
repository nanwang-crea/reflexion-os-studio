import type { SkillDefinition } from '../types.js'

/**
 * 验证-修复循环：coding agent 的核心工作模式——先验证，失败则
 * 分析→修复→再验证，直至通过或给出明确失败结论（带证据）。
 */
export const VERIFY_FIX_SKILL: SkillDefinition = {
  manifest: {
    id: 'verify-fix',
    name: '验证-修复循环',
    version: '1.0.0',
    description:
      '先运行测试/构建验证改动是否成立；失败则分析原因→修复→再验证，最多 4 轮，直至通过或给出带证据的失败结论。',
    tools: [
      'shell.execute',
      'file.read',
      'file.edit',
      'file.write',
      'file.grep',
    ],
    argumentHint: '<验证命令>；缺省时自动推断（npm test / pnpm test）',
  },
  instructions: `
# 验证-修复循环

先建立"验证命令"，再进入 修复 → 验证 循环，直到通过或无法修复。

## 开始

1. **定验证命令**：用户给了命令就用它；否则按项目惯例推断（先看 package.json scripts，其次 Makefile/pytest.ini 等），推断不出来就询问或说明不知道。
2. **跑第一次验证**：执行验证命令，记录退出码与关键输出。

## 循环（最多 4 轮）

3. **失败 → 分析**：读错误输出与相关源码，定位根因；确认不是环境/命令/参数问题（那种情况先修命令本身）。
4. **最小修复**：只改与根因相关的代码；避免顺手重构。记录改了什么。
5. **重跑验证**；通过则结束。

## 收尾

- **通过**：附上验证命令与最后一次通过的退出码/关键输出（成功证据）。
- **4 轮仍失败**：停止修复，输出——已尝试的修复列表、每轮结果、你判断的最可能根因与下一步建议；不得假装修复成功。
- 任何一轮验证命令本身失败（命令不存在/环境缺依赖），先排除该问题再继续循环，同样计入轮次。

## 纪律

- 验证输出太长时截取尾部 + 关键报错行；不要把整段日志塞回上下文。
- 不修改不相关的测试去"让测试通过"；改动测试须说明动机。
- 涉及单测覆盖缺口时，可补充针对性测试用例作为修复的一部分。
`.trim(),
}
