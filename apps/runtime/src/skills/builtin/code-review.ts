import type { SkillDefinition } from '../types.js'

/** 代码审查：先定位改动面，再精读，最后输出分级结论。 */
export const CODE_REVIEW_SKILL: SkillDefinition = {
  manifest: {
    id: 'code-review',
    name: '代码审查',
    version: '1.0.0',
    description:
      '审查工作区内指定文件或目录的代码质量：正确性、边界条件、可读性与明显性能问题，输出分级结论。',
    tools: ['file.glob', 'file.grep', 'file.read'],
    argumentHint: '<文件或目录路径> [审查重点]',
  },
  instructions: `
# 代码审查

针对用户指定的文件或目录执行一次结构化代码审查。

## 步骤

1. **确定范围**：目标是目录时先用 file.glob（如 \`src/**/*.ts\`）列出候选文件；
   用户未指定范围时先向用户确认，不要凭空猜测。
2. **建立上下文**：用 file.grep 定位关键入口（导出函数、路由、状态变更点），
   再用 file.read 精读相关文件；被审代码的调用方也要看，避免孤立判断。
3. **逐文件审查**，只报告有依据的问题，每条注明 文件:行号。

## 审查维度（按优先级）

- 正确性：逻辑错误、错误的边界条件、未处理的空值/异常路径
- 一致性：与项目既有约定冲突（命名、错误处理、模块边界）
- 可读性：误导性命名、过深嵌套、重复逻辑
- 性能：只在有明显问题（如循环内重复 IO）时提及

## 输出格式

用 Markdown 表格输出发现，列：严重度（高/中/低）、位置、问题、建议。
没有问题的维度不要凑数；结尾给一句总体评价。
若发现问题但用户未要求修复，只报告，不要擅自改文件。
`.trim(),
}
