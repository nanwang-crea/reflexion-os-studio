1. 将 `apps/desktop` 从 Electron 宿主改为 Tauri 应用：移除 Electron 主进程、preload 和 Electron 启动脚本/依赖，新增 Tauri 前端入口（`index.html`、轻量 TypeScript 页面）与 `src-tauri` Rust host 工程。保留现有中文 M0 启动页、900×600 窗口和状态展示，不引入尚不存在的 React/Chat 功能。
2. 在 Tauri Rust host 中实现最小安全 bridge 和 supervisor：注册白名单 command/event（获取 bootstrap 状态、发送 system ping、监听 bootstrap state/message），独立启动并监控 TypeScript Runtime 与 Rust System Runtime，保持 JSON-RPC 2.0 newline-delimited stdio、stdout 协议/stderr 日志、ready 状态聚合、Rust 缺失不阻塞 Chat、优雅 shutdown 与超时强制回收。Tauri host 与现有 `crates/system-runtime` 保持职责分离。
3. 解决开发/生产路径：开发模式从仓库构建产物启动 Node Runtime 和 Rust system runtime；生产模式把 Node Runtime 所需脚本/依赖与 Rust sidecar 声明为 Tauri resources，统一通过资源目录解析，并继续支持 `REFLEXION_SYSTEM_RUNTIME` 覆盖。若当前阶段无法安全打包 Node 运行时，将明确保留开发可运行路径并让生产构建在缺失资源时给出可诊断错误，而不伪造兼容性。
4. 增加 Tauri 配置、capabilities/权限白名单、macOS bundle 基础配置和 `.gitignore`/清理规则；更新根及 desktop 构建脚本为 `build:desktop`、`dev:desktop`、`build`/`test` 可识别 Tauri。保留通用 TypeScript packages/runtime 构建流程，移除 Electron-only `onlyBuiltDependencies` 和配置。
5. 更新架构/README 中的宿主描述，从 Electron 改为 Tauri，同时记录 Tauri host、TypeScript Runtime、Rust System Runtime 三者边界和当前 M0 限制。
6. 验证顺序：安装并锁定 Tauri/前端依赖；运行 Prettier、TypeScript typecheck/build；运行 `cargo fmt --check`、`cargo check`、`cargo test`；执行 Tauri 前端构建和可用的 `tauri build`/开发启动检查。最终报告成功项、受 macOS 签名/公证或 sidecar 资源条件影响而跳过的项。