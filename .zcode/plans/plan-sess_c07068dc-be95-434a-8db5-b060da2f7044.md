1. 在 `apps/runtime/src/handlers-workspace.ts` 与 `apps/runtime/src/system.ts` 的现有错误边界中保留具体错误信息，并在 `App.tsx` 将分支读取失败原因记录为可展示状态，而不是只显示笼统状态；检查 Runtime 未就绪、系统 Runtime 不可用及 Git 命令失败的路径，确保真正的 Git 错误不会被误报成“无分支”。
2. 核对 Rust `git.branches` 的仓库检测与命令结果处理，确保项目绑定目录确实作为 `current_dir` 使用；为分支读取失败补充足够诊断信息，不改变只读 Git 边界。
3. 将落地页的项目/分支选择器改成类似 ChatGPT 的 Composer 内嵌上下文控件：去掉明显的独立卡片边框、背景和大内边距，使用轻量文字/下拉样式，与对话输入框紧密相连；保留加载、读取失败、非 Git 仓库和无分支状态。
4. 运行格式、Lint、TypeScript 类型检查以及 Rust fmt/test，按检查结果修正问题并如实报告。