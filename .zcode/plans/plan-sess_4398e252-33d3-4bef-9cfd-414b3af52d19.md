1. 继续核对当前工作树已有改动与 git.rs 的完整实现，确认分支读取失败的实际错误路径，避免覆盖用户已有修改。
2. 以最小范围修复：增强 Rust `git.branches` 对工作区路径/非仓库/命令失败的判定与错误信息，确保合法非 Git 工作区返回 `repo=false`；同时检查前端新建对话加载状态是否需要防止旧请求结果污染。
3. 为根因补充或调整 Rust 单元测试（至少覆盖 Git 分支读取、非仓库/失败输出判定），必要时补充前端类型/行为相关验证，不引入超出 Phase 1B 只读 Git 范围的能力。
4. 运行项目规范要求的 format/lint/typecheck 及 Rust fmt/test；根据结果修复问题。
5. 逐项 review diff（架构边界、跨平台路径与进程调用、错误处理、现有未提交改动），确认无不相关变更后创建提交，并汇报原因、修复内容和验证结果。